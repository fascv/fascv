# control module

Scope:
- fastapi control plane
- telemetry/status UI endpoints

Primary runtime implementation remains in `trading/processes/control.py`.
Workspace API: `modules/control/src/trading_control/process.py`.
